
using UnityEngine;

public enum GameState
{
    Idle,
    Play,
    Finish,
    Over
}

public enum MotionType
{
    Move,
    LocalMove,
    Scale,
    Jump
}

public enum ShakeType
{
    Position,
    Rotation,
    Scale
}

public enum CollactableType
{
    Human,
    Coal
}